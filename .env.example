# Copy this file to ".env" and fill in your real key.
# ".env" is git-ignored — ".env.example" is the safe, committed placeholder
# that shows anyone cloning the repo what variables they need to supply.

ANTHROPIC_API_KEY=your_key_here
